# This macro handles relocatability and package prefix calculation.
@PACKAGE_INIT@

# 1. Include the Targets File
# This defines the imported targets like tp::tpack.
include("${CMAKE_CURRENT_LIST_DIR}/tpack-targets.cmake")

# 2. Check for required components
check_required_components(tpack)
